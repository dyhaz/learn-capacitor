import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'learn-capacitor',
  webDir: 'dist/learn-capacitor',
  bundledWebRuntime: false
};

export default config;
